from setuptools import setup

setup(
    name='breaking_bad_characters',
    version='0.1',
    description='A package where you can use Breaking Bad character info',
    long_description=open('README.md').read(),
    url='https://github.com/luka-danger/breaking-bad-characters.git',
    author='Luka Danger'
)